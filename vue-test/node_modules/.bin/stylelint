#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_stylelint@11.1.1@stylelint/bin/stylelint.js" "$@"
  ret=$?
else 
  node  "$basedir/../_stylelint@11.1.1@stylelint/bin/stylelint.js" "$@"
  ret=$?
fi
exit $ret
